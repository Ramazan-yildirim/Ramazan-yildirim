name: Generate Snake with Custom Colors

on:
  schedule:
    - cron: "0 0 * * *"  # Her gün çalıştır
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Generate snake animation
        uses: Platane/snk@v3
        with:
          github_user_name: Ramazan-yildirim
          outputs: dist/github-contribution-grid-snake.svg
          color_bg: '#0d1117'
          color_border: '#30363d'

      - name: Customize snake colors
        run: |
          python3 scripts/customize_snake_colors.py dist/github-contribution-grid-snake.svg

      - name: Push to output branch
        uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
